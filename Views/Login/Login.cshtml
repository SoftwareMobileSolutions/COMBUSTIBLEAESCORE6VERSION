@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>COMBUSTIBLEAESCORE</title>
    <link rel="stylesheet" href="~/lib/bootstrap/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/login.css" />

    <script src="~/js/jquery-3.3.1.min.js"></script>
    <script src="~/js/bootstrap.min.js"></script>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    <script src="~/lib/swalert/sweetalert2.min.js"></script>
    <link href="~/lib/swalert/sweetalert2.min.css" rel="stylesheet" />
    <script src="~/js/popper.min.js"></script>
    <link href="~/css/bootstrap.min.css" rel="stylesheet" />
    <link href="~/css/materialdatetimepicker.css" rel="stylesheet" />

    
</head>
<body>
    <section class="h-100 gradient-form" id="logIn">
        <div class="container-fluid  h-100">

            <div class="row d-flex  align-items-center h-100">
                <div class="col-xl-4 offset-lg-7">
                    <div class="card rounded-3 text-black fondo ">
                        <div class="row g-0">
                            <div class="col-lg-12">
                                <div class="card-body p-lg-5 ">

                                    <div class="text-center">
                                        <img src="~/images/logo.png"
                                             style="width: 185px;" alt="logo">
                                        <h4 class="mt-1 mb-5 pb-1">Combustible AES</h4>
                                    </div>

                                    <form>
                                        <p class="text-center">Inicio de cesión</p>

                                        <div class="form-outline mb-4">
                                            <label class="form-label" for="form2Example11">Nombre de usuario</label>
                                            <input type="email" id="username" class="form-control"
                                                   placeholder="Nombre de usuario" />
                                        </div>

                                        <!--<div class="form-outline mb-4">
                                            <label class="form-label" for="form2Example22">Contraseña</label>
                                            <input type="password" id="password" class="form-control" />
                                        </div>-->
                                        
                                        <div class="form-outline mb-4">
                                            <label class="form-label" for="">Contraseña</label>
                                            <div class="input-group border rounded p-0" id="show_hide_password">
                                                <input type="password" id="password" class="form-control border-0"
                                                       placeholder="Ingresa tu contraseña" />
                                                <div class="input-group-addon px-lg-2">
                                                    <a href="" class="align-middle mx-lg-auto">
                                                        <i class="bi bi-eye-fill align-middle mx-lg-auto" style="color:grey;"></i>
                                                    </a>
                                                </div>
                                            </div>
                                        </div>

                                        <div class="d-grid gap-2 col-lg-6 mx-auto">
                                            <button class="btn btn-light gradient-custom-2" type="button" id="login">
                                                Iniciar cesión
                                            </button>
                                            <a class="text-muted" href="#!">Olvidesate tu contraseña?</a>
                                        </div>

                                        <div class="d-flex align-items-center justify-content-center pb-4 mt-5">
                                            <p class="mb-0 me-2">No tienes una empreasa?</p>
                                            <a class="btn btn-outline-danger" href="@Url.Action("adRegistarCompany", "adRegistarCompany")">Crear nueva</a>
                                        </div>

                                    </form>

                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</body>
</html>


<script type="text/javascript">

    (function () {
        $.extend({
            login: {
                click: function () { 
                    var _this = this;
                    $("#login").on('click', function () {                      
                        $.get("/Login/iniciarSesion", { username: $("#username").val(), password: $("#password").val() }, function (data) {
                            //console.log(data);
                            if (data.estado == 1) {
                                //location.href = "Home"
                                window.location = "/Home/Index";
                            } else if (data.estado == 0) {
                                _this.alert("warning", "Usuario Inactivo", data.mensaje);
                                //location.href = "Login"

                            } else {
                                //console.log(data);
                                _this.alert("error", "Error", data.mensaje);
                            }

                        })
                    });
                },
                alert: function (icono, title, text) {
                    Swal.fire({
                        icon: icono,
                        title: title,
                        text: text,
                    });
                },
                showPassword: function () {
                    $("#show_hide_password a").on('click', function (event) {
                        event.preventDefault();
                        if ($('#show_hide_password input').attr("type") == "text") {
                            $('#show_hide_password input').attr('type', 'password');
                            $('#show_hide_password i').removeClass("bi bi-eye-slash-fill");
                            $('#show_hide_password i').addClass("bi bi-eye-fill");
                        } else if ($('#show_hide_password input').attr("type") == "password") {
                            $('#show_hide_password input').attr('type', 'text');
                            $('#show_hide_password i').addClass("bi bi-eye-slash-fill");
                            $('#show_hide_password i').removeClass("bi bi-eye-fill");

                        }
                    });
                }

            },
            home_init: function () {
                var _this = this;
                _this.login.click();
                _this.login.showPassword();
            }
        })

        $.home_init();
                  
    })();



    /*function () {
                $.get("Login/login", { username: $("#username").val(), password: $("#password").val() }, function (data) {
                    if (data.estado == 1) {
                        location.href = "Home"
                    } else if (data.estado == 0) {
                        location.href = "Login"
                        alert("Usuario inactico, solcite que lo active")
                    } else {
                        //console.log(data);
                        alert("Usuario y o contraseña incorrectos")
                    }
                });
            }, prueba: function () {

                console.log("Estas bien maje")
                $("#login").on('click', function (event) {

                    console.log("funciona, apurate pendejo XD");
                });
            }*/



    /*$("#login").on('click', function (event) {
        $.get("Login/login",

            { username: $("#username").val(), password: $("#password").val() },
            //{ username: "henryhrra", password: "Herrera201" },
            function (data) {
                //console.log(data);
                if (data.estado == 1) {
                    location.href = "Home"
                } else if (data.estado == 0) {
                    location.href = "Login"
                    alert("Usuario inactico, solcite que lo active")
                } else {
                    //console.log(data);
                    alert("Usuario y o contraseña incorrectos")
                }
                //if(data.)
            });
    });*/

</script>



